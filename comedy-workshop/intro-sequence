<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StarSeed Comedy Lounge - Intro Sequence</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(45deg, #ff0000, #0000ff, #ff6347);
            color: #fff;
            padding: 20px;
            line-height: 1.6;
            animation: gown-flow 10s infinite alternate;
            overflow-x: hidden;
        }
        @keyframes gown-flow {
            0% { background: linear-gradient(45deg, #ff0000, #0000ff, #ff6347); }
            50% { background: linear-gradient(45deg, #0000ff, #ff6347, #ff0000); }
            100% { background: linear-gradient(45deg, #ff6347, #ff0000, #0000ff); }
        }
        .scene-container {
            background: rgba(0, 0, 0, 0.8);
            padding: 30px;
            border-radius: 15px;
            max-width: 900px;
            margin: 0 auto;
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.3);
        }
        .scene-title {
            text-align: center;
            color: #ffd700;
            font-size: 2.5em;
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
            animation: title-glow 3s infinite alternate;
        }
        @keyframes title-glow {
            0% { text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8); }
            100% { text-shadow: 0 0 20px #ffd700, 2px 2px 4px rgba(0, 0, 0, 0.8); }
        }
        .intro-section {
            background: rgba(255, 215, 0, 0.1);
            padding: 25px;
            border-radius: 15px;
            margin: 20px 0;
            border: 2px solid #ffd700;
        }
        .character-intro {
            margin: 15px 0;
            padding: 15px;
            border-left: 4px solid #ff6347;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
        }
        .character-name {
            font-weight: bold;
            color: #ff69b4;
            font-size: 1.2em;
        }
        .character-description {
            margin-left: 20px;
            font-style: italic;
            color: #e0e0e0;
        }
        .stage-direction {
            color: #98fb98;
            font-style: italic;
            margin: 15px 0;
            text-align: center;
            font-size: 1.1em;
        }
        .sheep-nav {
            background: rgba(255, 215, 0, 0.2);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            margin: 20px 0;
        }
        .sheep-nav a {
            color: #ffd700;
            text-decoration: none;
            margin: 0 10px;
            font-weight: bold;
        }
        .sheep-nav a:hover {
            text-shadow: 0 0 10px #ff6347;
        }
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        .star {
            position: absolute;
            background: #fff;
            border-radius: 50%;
            animation: twinkle 3s infinite;
        }
        @keyframes twinkle {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 1; }
        }
        .duck {
            position: absolute;
            font-size: 2em;
            animation: quack-float 8s infinite;
        }
        @keyframes quack-float {
            0% { transform: translateX(0) translateY(0); }
            25% { transform: translateX(20px) translateY(-10px); }
            50% { transform: translateX(40px) translateY(5px); }
            75% { transform: translateX(20px) translateY(-5px); }
            100% { transform: translateX(0) translateY(0); }
        }
    </style>
</head>
<body>
    <div class="stars" id="starfield"></div>
    
    <div class="scene-container">
        <h1 class="scene-title">🎭 STARSEED COMEDY LOUNGE 🎭</h1>
        <h2 style="text-align: center; color: #ff69b4;">INTRO SEQUENCE: A Cosmic Comedy Begins</h2>
        
        <div class="stage-direction">
